# hyprtiki

Hyprland plugin that plays TIKI TIKI SLOWED in the background on loop.

```bash
cmake -B build && cmake --build build
hyprctl plugin load "$PWD/build/src/libhyprtiki.so"
hyprctl plugin unload "$PWD/build/src/libhyprtiki.so"
```
